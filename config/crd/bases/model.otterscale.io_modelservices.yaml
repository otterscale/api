---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: modelservices.model.otterscale.io
spec:
  group: model.otterscale.io
  names:
    categories:
    - otterscale
    kind: ModelService
    listKind: ModelServiceList
    plural: modelservices
    singular: modelservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .spec.model.name
      name: Model
      type: string
    - jsonPath: .status.decodeReady
      name: Decode
      priority: 1
      type: string
    - jsonPath: .status.prefillReady
      name: Prefill
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ModelService is the Schema for the modelservices API.
          A ModelService declares intent to serve an OCI-packaged model using vLLM,
          optionally with Prefill/Decode disaggregation and Gateway API integration.
          The model artifact is mounted via Kubernetes image volumes (requires K8s >= 1.35),
          eliminating the need for init containers or PVC-based model loading.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired model serving configuration.
            properties:
              accelerator:
                description: Accelerator configures the GPU/accelerator type for serving
                  pods.
                properties:
                  type:
                    description: |-
                      Type of accelerator hardware.
                      The operator uses this to determine the appropriate resource name
                      (e.g. nvidia.com/gpu, amd.com/gpu) and any accelerator-specific
                      environment variables.
                    enum:
                    - nvidia
                    - amd
                    - intel-gaudi
                    - google
                    - cpu
                    type: string
                required:
                - type
                type: object
              decode:
                description: |-
                  Decode configures the decode (or unified) serving pods.
                  In non-disaggregated mode, these are the only serving pods.
                properties:
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: NodeSelector constrains pods to nodes with matching
                      labels.
                    type: object
                  parallelism:
                    description: Parallelism configures vLLM tensor/data parallelism
                      for this role.
                    properties:
                      data:
                        default: 1
                        description: 'Data is the data-parallel-size: number of data-parallel
                          replicas within a single pod.'
                        format: int32
                        minimum: 1
                        type: integer
                      dataLocal:
                        default: 1
                        description: DataLocal is the data-parallel-size-local for
                          disaggregated serving.
                        format: int32
                        minimum: 1
                        type: integer
                      tensor:
                        default: 1
                        description: 'Tensor is the tensor-parallel-size: number of
                          GPUs used to shard a single model.'
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                  replicas:
                    default: 1
                    description: Replicas is the desired number of pod replicas.
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: |-
                      Resources for the vLLM container (CPU, memory).
                      GPU resources are automatically calculated from accelerator type
                      and parallelism (tensor * dataLocal) and should not be set manually.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  tolerations:
                    description: Tolerations for the serving pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                            Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              engine:
                description: Engine configures the inference engine (vLLM).
                properties:
                  args:
                    description: |-
                      Args are additional vLLM command-line arguments.
                      The operator automatically sets --model, --port, --served-model-name,
                      --tensor-parallel-size, and --data-parallel-size based on the spec.
                      User-provided args are appended after the auto-generated ones.
                    items:
                      type: string
                    type: array
                  env:
                    description: Env are additional environment variables for the
                      engine container.
                    items:
                      description: EnvVar represents an environment variable present
                        in a Container.
                      properties:
                        name:
                          description: |-
                            Name of the environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        value:
                          description: |-
                            Variable references $(VAR_NAME) are expanded
                            using the previously defined environment variables in the container and
                            any service environment variables. If a variable cannot be resolved,
                            the reference in the input string will be unchanged. Double $$ are reduced
                            to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                            "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                            Escaped references will never be expanded, regardless of whether the variable
                            exists or not.
                            Defaults to "".
                          type: string
                        valueFrom:
                          description: Source for the environment variable's value.
                            Cannot be used if value is not empty.
                          properties:
                            configMapKeyRef:
                              description: Selects a key of a ConfigMap.
                              properties:
                                key:
                                  description: The key to select.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the ConfigMap or its
                                    key must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                            fieldRef:
                              description: |-
                                Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                              properties:
                                apiVersion:
                                  description: Version of the schema the FieldPath
                                    is written in terms of, defaults to "v1".
                                  type: string
                                fieldPath:
                                  description: Path of the field to select in the
                                    specified API version.
                                  type: string
                              required:
                              - fieldPath
                              type: object
                              x-kubernetes-map-type: atomic
                            fileKeyRef:
                              description: |-
                                FileKeyRef selects a key of the env file.
                                Requires the EnvFiles feature gate to be enabled.
                              properties:
                                key:
                                  description: |-
                                    The key within the env file. An invalid key will prevent the pod from starting.
                                    The keys defined within a source may consist of any printable ASCII characters except '='.
                                    During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                  type: string
                                optional:
                                  default: false
                                  description: |-
                                    Specify whether the file or its key must be defined. If the file or key
                                    does not exist, then the env var is not published.
                                    If optional is set to true and the specified key does not exist,
                                    the environment variable will not be set in the Pod's containers.

                                    If optional is set to false and the specified key does not exist,
                                    an error will be returned during Pod creation.
                                  type: boolean
                                path:
                                  description: |-
                                    The path within the volume from which to select the file.
                                    Must be relative and may not contain the '..' path or start with '..'.
                                  type: string
                                volumeName:
                                  description: The name of the volume mount containing
                                    the env file.
                                  type: string
                              required:
                              - key
                              - path
                              - volumeName
                              type: object
                              x-kubernetes-map-type: atomic
                            resourceFieldRef:
                              description: |-
                                Selects a resource of the container: only resources limits and requests
                                (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                              properties:
                                containerName:
                                  description: 'Container name: required for volumes,
                                    optional for env vars'
                                  type: string
                                divisor:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  description: Specifies the output format of the
                                    exposed resources, defaults to "1"
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                resource:
                                  description: 'Required: resource to select'
                                  type: string
                              required:
                              - resource
                              type: object
                              x-kubernetes-map-type: atomic
                            secretKeyRef:
                              description: Selects a key of a secret in the pod's
                                namespace
                              properties:
                                key:
                                  description: The key of the secret to select from.  Must
                                    be a valid secret key.
                                  type: string
                                name:
                                  default: ""
                                  description: |-
                                    Name of the referent.
                                    This field is effectively required, but due to backwards compatibility is
                                    allowed to be empty. Instances of this type with an empty value here are
                                    almost certainly wrong.
                                    More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                  type: string
                                optional:
                                  description: Specify whether the Secret or its key
                                    must be defined
                                  type: boolean
                              required:
                              - key
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  image:
                    default: ghcr.io/llm-d/llm-d-cuda:v0.5.1
                    description: Image is the vLLM container image.
                    type: string
                  imagePullPolicy:
                    description: ImagePullPolicy for the engine container.
                    type: string
                  port:
                    default: 8000
                    description: |-
                      Port is the port the inference engine listens on.
                      When a routing proxy is enabled, this is the external port exposed by the proxy,
                      and vLLM listens on routingProxy.targetPort instead.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
              httpRoute:
                description: |-
                  HTTPRoute optionally creates a Gateway API HTTPRoute pointing to the InferencePool.
                  Requires InferencePool to be set.
                properties:
                  gatewayRef:
                    description: GatewayRef references the Gateway to attach the HTTPRoute
                      to.
                    properties:
                      name:
                        description: Name of the Gateway.
                        minLength: 1
                        type: string
                      namespace:
                        description: Namespace of the Gateway. If empty, defaults
                          to the ModelService namespace.
                        type: string
                    required:
                    - name
                    type: object
                  hostnames:
                    description: Hostnames for the HTTPRoute. If empty, the route
                      matches all hostnames.
                    items:
                      type: string
                    type: array
                required:
                - gatewayRef
                type: object
              inferencePool:
                description: |-
                  InferencePool configures the Gateway API Inference Extension InferencePool.
                  When set, the operator creates and manages an InferencePool resource
                  with selector labels matching the serving pods.
                properties:
                  endpointPicker:
                    description: |-
                      EndpointPicker configures the Endpoint Picker extension deployment.
                      The operator creates and manages the EPP Deployment, Service, and
                      supporting resources alongside the InferencePool.
                    properties:
                      failureMode:
                        default: FailOpen
                        description: FailureMode determines behavior when the EPP
                          is unavailable.
                        enum:
                        - FailOpen
                        - FailClose
                        type: string
                      image:
                        default: ghcr.io/llm-d/llm-d-inference-scheduler:v0.6.0
                        description: Image is the EPP container image.
                        type: string
                      port:
                        default: 9002
                        description: Port of the EPP service (extProc gRPC port).
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      replicas:
                        default: 1
                        description: Replicas is the number of EPP pod replicas.
                        format: int32
                        minimum: 0
                        type: integer
                      resources:
                        description: |-
                          Resources for the EPP container (CPU, memory).
                          CPU limits are intentionally left unset by default to allow bursting
                          during scheduling spikes.
                        properties:
                          claims:
                            description: |-
                              Claims lists the names of resources, defined in spec.resourceClaims,
                              that are used by this container.

                              This field depends on the
                              DynamicResourceAllocation feature gate.

                              This field is immutable. It can only be set for containers.
                            items:
                              description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                              properties:
                                name:
                                  description: |-
                                    Name must match the name of one entry in pod.spec.resourceClaims of
                                    the Pod where this field is used. It makes that resource available
                                    inside a container.
                                  type: string
                                request:
                                  description: |-
                                    Request is the name chosen for a request in the referenced claim.
                                    If empty, everything from the claim is made available, otherwise
                                    only the result of this request.
                                  type: string
                              required:
                              - name
                              type: object
                            type: array
                            x-kubernetes-list-map-keys:
                            - name
                            x-kubernetes-list-type: map
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                    type: object
                required:
                - endpointPicker
                type: object
              model:
                description: Model defines the OCI model artifact and serving identity.
                properties:
                  image:
                    description: |-
                      Image is the OCI reference for the model artifact.
                      It is used as a Kubernetes image volume source, leveraging node-level
                      container image caching for efficient model distribution.
                      Example: "registry.example.com/models/qwen3-32b:v1"
                    minLength: 1
                    type: string
                  imagePullSecrets:
                    description: ImagePullSecrets for pulling the model OCI artifact
                      from a private registry.
                    items:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    type: array
                  mountPath:
                    default: /models
                    description: |-
                      MountPath is where the model artifact is mounted in serving containers.
                      The vLLM --model argument is automatically set to this path.
                    type: string
                  name:
                    description: |-
                      Name is the model identifier used in OpenAI-compatible API requests
                      (e.g. "qwen/Qwen3-32B", "meta-llama/Llama-3-70B-Instruct").
                      This is passed to vLLM as --served-model-name.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - image
                - name
                type: object
              monitoring:
                description: Monitoring configures observability features.
                properties:
                  podMonitor:
                    description: PodMonitor configures Prometheus PodMonitor creation
                      for serving pods.
                    properties:
                      enabled:
                        default: false
                        description: Enabled controls PodMonitor creation.
                        type: boolean
                      interval:
                        default: 30s
                        description: Interval between scrapes.
                        type: string
                      path:
                        default: /metrics
                        description: Path is the HTTP endpoint to scrape metrics from.
                        type: string
                      portName:
                        default: http
                        description: PortName to scrape metrics from. Must match a
                          named port on the vLLM container.
                        type: string
                    type: object
                type: object
              prefill:
                description: |-
                  Prefill optionally configures separate prefill pods for disaggregated serving.
                  When set, the serving architecture splits into Prefill (prompt processing)
                  and Decode (token generation) phases on separate pod groups.
                properties:
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: NodeSelector constrains pods to nodes with matching
                      labels.
                    type: object
                  parallelism:
                    description: Parallelism configures vLLM tensor/data parallelism
                      for this role.
                    properties:
                      data:
                        default: 1
                        description: 'Data is the data-parallel-size: number of data-parallel
                          replicas within a single pod.'
                        format: int32
                        minimum: 1
                        type: integer
                      dataLocal:
                        default: 1
                        description: DataLocal is the data-parallel-size-local for
                          disaggregated serving.
                        format: int32
                        minimum: 1
                        type: integer
                      tensor:
                        default: 1
                        description: 'Tensor is the tensor-parallel-size: number of
                          GPUs used to shard a single model.'
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                  replicas:
                    default: 1
                    description: Replicas is the desired number of pod replicas.
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: |-
                      Resources for the vLLM container (CPU, memory).
                      GPU resources are automatically calculated from accelerator type
                      and parallelism (tensor * dataLocal) and should not be set manually.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  tolerations:
                    description: Tolerations for the serving pods.
                    items:
                      description: |-
                        The pod this Toleration is attached to tolerates any taint that matches
                        the triple <key,value,effect> using the matching operator <operator>.
                      properties:
                        effect:
                          description: |-
                            Effect indicates the taint effect to match. Empty means match all taint effects.
                            When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: |-
                            Key is the taint key that the toleration applies to. Empty means match all taint keys.
                            If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                          type: string
                        operator:
                          description: |-
                            Operator represents a key's relationship to the value.
                            Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                            Exists is equivalent to wildcard for value, so that a pod can
                            tolerate all taints of a particular category.
                            Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                          type: string
                        tolerationSeconds:
                          description: |-
                            TolerationSeconds represents the period of time the toleration (which must be
                            of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                            it is not set, which means tolerate the taint forever (do not evict). Zero and
                            negative values will be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: |-
                            Value is the taint value the toleration matches to.
                            If the operator is Exists, the value should be empty, otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              routingProxy:
                description: |-
                  RoutingProxy configures the llm-d routing sidecar for disaggregated serving.
                  Required when Prefill is set; the proxy routes prefill requests between pods.
                properties:
                  certPath:
                    description: CertPath is the path to TLS certificates for the
                      routing proxy.
                    type: string
                  connector:
                    default: nixlv2
                    description: Connector specifies the KV-cache transfer protocol.
                    type: string
                  image:
                    default: ghcr.io/llm-d/llm-d-routing-sidecar:v0.6.0
                    description: Image is the routing proxy container image.
                    type: string
                  prefillerUseTLS:
                    description: PrefillerUseTLS enables TLS for prefiller communication.
                    type: boolean
                  secureProxy:
                    description: SecureProxy enables TLS on the routing proxy.
                    type: boolean
                  targetPort:
                    default: 8200
                    description: |-
                      TargetPort is the port where vLLM actually listens when the proxy is enabled.
                      The proxy intercepts on engine.port and forwards to this port.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  zapEncoder:
                    description: ZapEncoder sets the Zap log encoding format (e.g.
                      "json", "console").
                    type: string
                  zapLogLevel:
                    description: ZapLogLevel sets the Zap log level (e.g. "debug",
                      "info", "error").
                    type: string
                type: object
            required:
            - accelerator
            - decode
            - engine
            - model
            type: object
          status:
            description: Status represents the current state of the model serving
              deployment.
            properties:
              conditions:
                description: Conditions store the status conditions of the ModelService.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              decodeReady:
                description: DecodeReady is the number of ready decode replicas.
                format: int32
                type: integer
              decodeReplicas:
                description: DecodeReplicas is the desired number of decode replicas.
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase is the high-level summary of the ModelService lifecycle.
                type: string
              prefillReady:
                description: PrefillReady is the number of ready prefill replicas.
                format: int32
                type: integer
              prefillReplicas:
                description: PrefillReplicas is the desired number of prefill replicas.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
